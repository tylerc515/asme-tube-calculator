# Security

To report a security issue, open a GitHub issue or email the repository
owner directly via the contact on the GitHub profile.

This is a static client-side web application. It has no backend, no
authentication, and no user data is transmitted or stored externally.
All calculation logic runs in the browser.
