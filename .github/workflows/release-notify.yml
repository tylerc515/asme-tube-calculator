name: Notify Discord on Release

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: Release tag to announce (e.g. v1.0.0)
        required: true
      body:
        description: Release notes body (markdown, same format as a GitHub release)
        required: true

permissions:
  contents: read

jobs:
  notify:
    runs-on: ubuntu-latest
    if: ${{ !github.event.release.prerelease }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: '3.12'

      - name: Send release notification
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          DISCORD_ANALYST_ROLE_ID: ${{ secrets.DISCORD_ANALYST_ROLE_ID }}
          RELEASE_TAG: ${{ github.event.release.tag_name || inputs.version }}
          RELEASE_BODY: ${{ github.event.release.body || inputs.body }}
        run: python scripts/notify_release.py
